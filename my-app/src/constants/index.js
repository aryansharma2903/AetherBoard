// JavaScript (actually, tools like Webpack, Vite, or Node.js) will:

// Look inside the constants folder.

// Automatically look for a file called index.js.

// Use whatever is exported from that file.
export { toolTypes } from "./toolType";
export { actions } from './action';
export { cursorPositions } from './cursorPosition';